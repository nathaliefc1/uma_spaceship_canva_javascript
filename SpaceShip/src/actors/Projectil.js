/*import Spaceship from "../Spaceship";
/*addEventListener("click", (event) => {
    const projectile = new projectile(
      canvas/
    )
  }

const Projectile {
    constructor (pos = {x: Spaceship(pos.x), y: Spaceship(pos.y)} {

      
    }

    draw(x, y, r, color) =>{
      ctx.fillStyle = color;

    }
}*/
